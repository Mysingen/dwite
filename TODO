Make the wire detect when the other end of the socket is closed.

Test suites...

Can streamer use wire?

Playlists?

Do something about menu population. Way too slow with thousands of entries.

Add file:line printouts to all exception printouts.

NowPlaying render blacks out if you keep arrow keys pressed and the render is supposed to remain in view after the key press has been handled. E.g. if currently playing track is at the head of a menu and the user keeps UP pressed.

Volume can be taken too high. Or at least something is wrong with the way it currently works because sound breaks up if you raise it.

